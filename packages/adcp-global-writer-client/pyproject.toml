[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "adcp-global-writer-client"
version = "0.5.0"
description = "Narrow Global Production Writer coordination client"
requires-python = ">=3.12,<3.14"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
